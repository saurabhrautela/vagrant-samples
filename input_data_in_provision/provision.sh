#!/bin/bash

# SAMPLE USE-CASE
# login to pull images from some-registry.domain.com
# docker login --username=${username} --password=${password} some-registry.domain.com

echo ${username}
echo ${password}
